
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <!-- This is for the BottomSheetDialog. -->
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <!-- This is for the DatePicker -->
        <item name="android:datePickerStyle">@style/MyDatePicker</item>

    </style>

    <style name="SpinnerTheme_Mentor">
        <item name="android:fontFamily">@font/applesdgothicneom</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/blackTitle</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/round_rect</item>
    </style>

    <style name="MyPopUp" parent="Widget.AppCompat.PopupMenu">
        <item name="android:textColor">@color/blackTitle</item>
        <item name="android:itemBackground">@drawable/selector_popup</item>
        <item name="android:fontFamily">@font/applesdgothicneom</item>
        <item name="android:textSize">12sp</item>
    </style>

    <style name="MyDatePicker" parent="android:Widget.Material.Light.DatePicker">
        <item name="android:datePickerMode">spinner</item>
        <item name="android:fontFamily">@font/applesdgothicneom</item>
    </style>
</resources>
